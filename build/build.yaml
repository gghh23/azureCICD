trigger:
- master

pool:
  vmImage: 'Runner-azure'

steps:
- script: |
    echo 'Building the Docker image'
    docker rmi $(docker images -a -q)
    docker build . -t nignxazurecicd:version1
  displayName: 'Build Docker Image'
