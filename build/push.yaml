trigger:
- master

pool:
  name: 'default'
  vmImage: 'azure-agent'

steps:

- script: |
    docker login -u $(DockerCred.dockerId) -p $(DockerCred.pswd) nignxazurecicd.azurecr.io
    docker push nignxazurecicd:version1
  displayName: 'Build and Push to acr'
